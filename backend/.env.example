# MongoDB connection string (MongoDB Atlas free tier or local)
MONGODB_URI=mongodb://127.0.0.1:27017/bharat-darshan

# Secret used to sign JWTs - use a long random string in production
JWT_SECRET=change-me-to-a-long-random-string

# Port the API listens on
PORT=5000

# Customer care number shown in chatbot, footer and About page
SUPPORT_PHONE=8502947105

# Allowed frontend origin(s), comma separated
CLIENT_URL=http://localhost:5173

# Seed script admin account
ADMIN_EMAIL=admin@bharatdarshan.local
ADMIN_PASSWORD=ChangeMe123!

# Optional: enable LLM fallback for the chatbot (works without it)
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
